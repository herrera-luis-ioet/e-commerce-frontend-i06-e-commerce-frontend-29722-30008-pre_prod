/**
 * Assets Index
 * 
 * Export all assets from the assets directory
 */

// Image assets
import productPlaceholder from './images/placeholder.svg';

// Icon assets
import iconPlaceholder from './icons/placeholder.svg';

// Export image assets
export const images = {
  productPlaceholder,
};

// Export icon assets
export const icons = {
  placeholder: iconPlaceholder,
};

// Default export for convenience
export default {
  images,
  icons,
};